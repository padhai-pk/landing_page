# Padhai landing page — frontend env (Vite)
# Copy to .env and adjust for local dev / production.

# Backend API base URL (no trailing slash). Required for waitlist signups + subjects.
VITE_BACKEND_URL=http://127.0.0.1:8000

# Production example:
# VITE_BACKEND_URL=https://your-backend.vercel.app
